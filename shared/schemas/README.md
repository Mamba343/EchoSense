# Shared Schemas

This directory is reserved for shared schemas and common type definitions.

Planned contents:

- CSI frame schema
- live WebSocket message schema
- dashboard state schema
- dataset metadata schema
- calibration metadata schema

Schemas should be EchoSense-specific and should not copy external project schemas.
