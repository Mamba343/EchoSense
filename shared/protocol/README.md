# Shared Protocol

This directory is reserved for EchoSense protocol definitions.

Planned contents:

- UDP CSI packet format
- WebSocket message envelope
- REST API contract notes
- protocol versioning rules

Firmware, backend, simulator, replay mode, and frontend should use these definitions as the source of truth.
