# Shared

Shared project definitions live here.

This directory is reserved for protocol definitions, packet schemas, constants, common types, and future serialization helpers used by firmware, backend, and frontend. These files should define EchoSense-specific contracts so each layer does not invent duplicate packet names, message names, state names, or constants.

No runtime implementation belongs here yet.
