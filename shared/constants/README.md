# Shared Constants

This directory is reserved for shared constants.

Planned contents:

- protocol version constants
- dashboard state names
- WebSocket message type names
- default ports
- dataset format versions

Constants should be documented before being used by firmware, backend, or frontend.
